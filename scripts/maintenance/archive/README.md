# Maintenance Archive

This directory stores historical prototypes, abandoned view experiments, and
other files that were previously left inside runtime-facing directories such as
`resources/views` or `public`.

These files are intentionally kept out of the active Laravel runtime to reduce
confusion during maintenance. They are preserved only as archival references.
